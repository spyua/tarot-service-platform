// Static data (tarot cards, interpretations, etc.)
// Export data as it is created

import tarotCards from './tarotCards.json';
import { interpretationFrameworks } from './interpretationFrameworks';

export { tarotCards, interpretationFrameworks };
